<!-- Top Bar -->
<div class="top-bar bg-dark text-white py-2 d-none d-md-block">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-8">
                <div class="d-flex align-items-center">
                    <div class="me-4">
                        <i class="fas fa-phone me-2"></i>
                        <span>Hotline: 1900-1234</span>
                    </div>
                    <div class="me-4">
                        <i class="fas fa-envelope me-2"></i>
                        <span>info@laptopstore.com</span>
                    </div>
                    <div>
                        <i class="fas fa-clock me-2"></i>
                        <span>Mon - Sat: 8AM - 10PM</span>
                    </div>
                </div>
            </div>
            <div class="col-md-4 text-end">
                <div class="social-links">
                    <a href="#" class="text-white me-3" data-bs-toggle="tooltip" title="Facebook">
                        <i class="fab fa-facebook-f"></i>
                    </a>
                    <a href="#" class="text-white me-3" data-bs-toggle="tooltip" title="Twitter">
                        <i class="fab fa-twitter"></i>
                    </a>
                    <a href="#" class="text-white me-3" data-bs-toggle="tooltip" title="Instagram">
                        <i class="fab fa-instagram"></i>
                    </a>
                    <a href="#" class="text-white" data-bs-toggle="tooltip" title="YouTube">
                        <i class="fab fa-youtube"></i>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Main Header -->
<nav class="navbar navbar-expand-lg navbar-light bg-white py-3">
    <div class="container-fluid">
        <!-- Brand Logo -->
        <a class="navbar-brand logo-text" href="@Url.Action("Index", "Home")">
            CỬA HÀNG LAPTOP
        </a>

        <!-- Mobile Toggle Button -->
        <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navigation Menu -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="@Url.Action("Index", "Home")">
                        <i class="fas fa-home me-1"></i>
                        Trang chủ
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="storeDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-store me-1"></i>
                        Cửa hàng
                    </a>
                    <ul class="dropdown-menu shadow border-0 rounded-3">
                        @Html.Action("GetDanhMuc", "Home")
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("About","Home")">
                        <i class="fas fa-info-circle me-1"></i>
                        Về chúng tôi
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Contact","Home")">
                        <i class="fas fa-phone-alt me-1"></i>
                        Liên hệ
                    </a>
                </li>
            </ul>

            <!-- Right Side Menu -->
            <div class="navbar-nav ms-auto d-flex align-items-center">
                <!-- Shopping Cart -->
                <div class="nav-item me-4">
                    <a href="@Url.Action("GioHang", "Home")" class="btn btn-outline-primary rounded-pill position-relative">
                        <i class="fas fa-shopping-bag"></i>
                        <span class="d-none d-lg-inline ms-2">Giỏ hàng</span>
                    </a>
                </div>

                <!-- User Account -->
                <div class="nav-item dropdown">
                    @if (Session["Username"] != null)
                    {
                        <button class="btn btn-outline-success rounded-pill" type="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user me-1"></i>
                            <span class="d-none d-lg-inline">Xin chào, @Session["FullName"]</span>
                            <i class="fas fa-chevron-down ms-1"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end shadow border-0 rounded-3">
                            <li><h6 class="dropdown-header">Tài khoản của bạn</h6></li>
                            <li>
                                <a class="dropdown-item" href="@Url.Action("HoSo", "Home")">
                                    <i class="fas fa-user-circle me-2"></i>Xem hồ sơ
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="@Url.Action("Chat", "Home")">
                                    <i class="fas fa-comments me-2"></i>Chat với nhân viên
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="@Url.Action("DonHangCuaToi", "Home")">
                                    <i class="fas fa-shopping-bag"></i> Đơn hàng của tôi
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="@Url.Action("DanhSachDoiTra", "Home")">
                                    <i class="fas fa-shopping-bag"></i> Danh sách đổi trả
                                </a>
                            </li>
                            <!-- Link đến Bảo hành -->
                            <li>
                                <a class="dropdown-item" href="@Url.Action("DanhSachBaoHanhKhachHang", "BaoHanh")">
                                    <i class="fas fa-shield-alt"></i> Phiếu bảo hành
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="@Url.Action("DangXuat", "Home")">
                                    <i class="fas fa-sign-out-alt me-2"></i>Đăng xuất
                                </a>
                            </li>
                        </ul>
                    }
                    else
                    {
                        <button class="btn btn-outline-primary rounded-pill" type="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user me-1"></i>
                            <span class="d-none d-lg-inline">Tài khoản</span>
                            <i class="fas fa-chevron-down ms-1"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end shadow border-0 rounded-3">
                            <li><h6 class="dropdown-header">Chào mừng bạn!</h6></li>
                            <li>
                                <a class="dropdown-item" href="@Url.Action("DangKy", "Home")">
                                    <i class="fas fa-user-plus me-2"></i>Đăng ký
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="@Url.Action("DangNhap", "Home")">
                                    <i class="fas fa-sign-in-alt me-2"></i>Đăng nhập
                                </a>
                            </li>
                        </ul>
                    }
                </div>
            </div>
        </div>
    </div>
</nav>

<style>
    /* Header Custom Styles */
    .top-bar {
        font-size: 0.875rem;
    }

    .social-links a {
        transition: all 0.3s ease;
        font-size: 1.1rem;
    }

        .social-links a:hover {
            color: var(--primary-color) !important;
            transform: translateY(-2px);
        }

    .navbar {
        transition: all 0.3s ease;
    }

        .navbar.scrolled {
            box-shadow: 0 2px 20px rgba(0,0,0,0.15);
        }

    .navbar-brand {
        transition: all 0.3s ease;
    }

        .navbar-brand:hover {
            transform: scale(1.05);
        }

    .navbar-nav .nav-link {
        font-weight: 500;
        padding: 0.75rem 1rem !important;
        transition: all 0.3s ease;
        position: relative;
        border-radius: 8px;
    }

        .navbar-nav .nav-link:hover {
            color: var(--primary-color) !important;
            background-color: rgba(13, 110, 253, 0.1);
        }

    .dropdown-menu {
        border: none;
        box-shadow: 0 10px 40px rgba(0,0,0,0.15);
        border-radius: 12px !important;
        padding: 1rem 0;
    }

    .dropdown-item {
        padding: 0.5rem 1.5rem;
        transition: all 0.3s ease;
        border-radius: 8px;
        margin: 0 0.5rem;
    }

        .dropdown-item:hover {
            background-color: rgba(13, 110, 253, 0.1);
            color: var(--primary-color);
        }

    .search-dropdown {
        min-width: 350px !important;
    }

    .cart-dropdown {
        min-width: 400px !important;
        max-width: 400px;
    }

    .cart-item-image {
        border-radius: 8px;
    }

    .badge {
        font-size: 0.7rem;
    }

    /* Mobile Responsive */
    media (max-width: 991.98px) {
        .navbar-nav .nav-link

    {
        padding: 0.5rem 1rem !important;
    }

    .navbar-nav .dropdown-menu {
        border: 1px solid #dee2e6;
        margin-top: 0.5rem;
    }

    .cart-dropdown,
    .search-dropdown {
        min-width: 300px !important;
    }

    }

    media (max-width: 576px) {
        .top-bar

    {
        font-size: 0.75rem;
    }

    .top-bar .col-md-8 > div {
        flex-direction: column;
        align-items: flex-start !important;
    }

        .top-bar .col-md-8 > div > div {
            margin-bottom: 0.25rem;
            margin-right: 0 !important;
        }

    }

    /* Animation for mobile menu */
    .navbar-collapse {
        transition: all 0.3s ease;
    }

    /* Sticky header effect */
    .navbar.sticky {
        background: rgba(255, 255, 255, 0.95) !important;
        backdrop-filter: blur(10px);
    }

    /* Cart and search dropdown animations */
    .dropdown-menu {
        animation: fadeInUp 0.3s ease;
    }

    keyframes fadeInUp {
        from

    {
        opacity: 0;
        transform: translateY(10px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
    }
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Sticky header effect
    const navbar = document.querySelector('.navbar');
    let lastScrollTop = 0;

    window.addEventListener('scroll', function() {
        let scrollTop = window.pageYOffset || document.documentElement.scrollTop;

        if (scrollTop > 100) {
            navbar.classList.add('sticky');
        } else {
            navbar.classList.remove('sticky');
        }

        lastScrollTop = scrollTop;
    });

    // Close mobile menu when clicking on a link
    document.querySelectorAll('.navbar-nav .nav-link').forEach(link => {
        link.addEventListener('click', function() {
            const navbarCollapse = document.querySelector('.navbar-collapse');
            if (navbarCollapse.classList.contains('show')) {
                bootstrap.Collapse.getInstance(navbarCollapse).hide();
            }
        });
    });

    // Search functionality
    const searchForm = document.querySelector('.search-dropdown form');
    if (searchForm) {
        searchForm.addEventListener('submit', function(e) {
            e.preventDefault();
            const searchTerm = this.querySelector('input').value;
            if (searchTerm.trim()) {
                // Implement search functionality here
                console.log('Searching for:', searchTerm);
                // window.location.href = '/Search?q=' + encodeURIComponent(searchTerm);
            }
        });
    }

    // Cart item removal - DISABLED: Handled by GioHang.cshtml with confirmation modal
    // This code was causing items to be deleted immediately before confirmation
    // document.querySelectorAll('.cart-item .btn-outline-danger').forEach(button => {
    //     button.addEventListener('click', function() {
    //         const cartItem = this.closest('.cart-item');
    //         cartItem.style.animation = 'fadeOut 0.3s ease';
    //         setTimeout(() => {
    //             cartItem.remove();
    //             // Update cart count and total
    //             updateCartInfo();
    //         }, 300);
    //     });
    // });

    function updateCartInfo() {
        // Update cart count and total price
        // This would typically make an AJAX call to update the server-side cart
        console.log('Cart updated');
    }
});
</script>